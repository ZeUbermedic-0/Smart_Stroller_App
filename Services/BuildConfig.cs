namespace Smart_Stroller_App.Services
{
    public static class BuildConfig
    {
        // Flip to false to hit a real backend (BaseUrl in ApiClient)
        public static bool UseMockApi { get; set; } = true;
    }
}
